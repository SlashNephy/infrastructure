apiVersion: v1
kind: Service
metadata:
  name: public

spec:
  selector:
    app: public
  ports:
    - port: 5572
      targetPort: 5572

---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: public

spec:
  routes:
    - kind: Rule
      match: Host(`rclone.starry.blue`) && PathPrefix(`/public`)
      priority: 10
      services:
        - name: public
          port: 5572
      middlewares:
        - name: forward-auth-authentik
          namespace: authentik
        - name: rclone-authorization-header
