apiVersion: v1
kind: Service
metadata:
  name: app

spec:
  selector:
    app: default
  type: NodePort
  ipFamilyPolicy: PreferDualStack
  ipFamilies:
    - IPv4
    - IPv6
  ports:
    - name: smb
      port: 445
      targetPort: smb
      nodePort: 445
      protocol: TCP
