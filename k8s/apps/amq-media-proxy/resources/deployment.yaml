apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment

spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: app
  template:
    metadata:
      annotations:
        fluentbit.io/exclude: "true"
      labels:
        app: app
    spec:
      containers:
        - name: app
          image: ghcr.io/slashnephy/amq-media-proxy:master@sha256:94749e667814b373b2cb44731b4805e8ee9576bcc85abae5842392e1f3e59bf5
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: app-volume
              mountPath: /app/.cache
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: app-volume
          hostPath:
            path: /opt/k8s-volumes/amq-media-proxy/cache
            type: DirectoryOrCreate
      restartPolicy: Always
