general:
  backend: Mirakurun
  mirakurun_url: "http://app.mahiron:40772/"
  encoder: QSVEncC
  debug: true
  debug_encoder: true

tv:
  max_alive_time: 60

video:
  recorded_folders:
    - /mnt/records
    - /mnt/records_local

capture:
  upload_folders:
    - /code/server/data/capture
