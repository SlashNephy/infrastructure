apiVersion: v1
kind: Service
metadata:
  name: service

spec:
  selector:
    app: default
  ports:
    - name: dns
      port: 53
      targetPort: 53
    - name: http
      port: 80
      targetPort: 80
