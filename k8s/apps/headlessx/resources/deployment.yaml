apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment

spec:
  replicas: 1
  selector:
    matchLabels:
      app: default
  template:
    metadata:
      labels:
        app: default
    spec:
      initContainers:
        - name: db-push
          image: ghcr.io/slashnephy/headlessx-api:2.0.4@sha256:25956d10a173b00921adbc14ce37b7452e35d349f818eb2aca14ada22a5a3e89
          command:
            - pnpm
            - exec
            - prisma
            - db
            - push
          envFrom:
            - secretRef:
                name: headlessx-secret
          securityContext:
            allowPrivilegeEscalation: false
      containers:
        - name: api
          image: ghcr.io/slashnephy/headlessx-api:2.0.4@sha256:25956d10a173b00921adbc14ce37b7452e35d349f818eb2aca14ada22a5a3e89
          # 依存関係が不足している問題に対する一時的なワークアラウンド
          # https://github.com/saifyxpro/HeadlessX/issues/39#issuecomment-4058155138
          command:
            - bash
            - -euxc
            - |
              apt-get update
              apt-get install -y --no-install-recommends libgtk-3-0 libx11-xcb1
              exec pnpm start
          ports:
            - name: api
              containerPort: 8000
          env:
            - name: PORT
              value: "8000"
            - name: HOST
              value: 0.0.0.0
            - name: NODE_ENV
              value: production
            - name: FRONTEND_URL
              value: https://headlessx.starry.blue
            - name: LOG_LEVEL
              value: debug
            # コンテナ内で Firefox サンドボックスが /proc/self/uid_map に書き込めず子プロセスがクラッシュする問題の回避
            - name: MOZ_DISABLE_CONTENT_SANDBOX
              value: "1"
            - name: TZ
              value: Asia/Tokyo
          envFrom:
            - secretRef:
                name: headlessx-secret
          volumeMounts:
            - name: models
              mountPath: /app/apps/api/models
            - name: data
              mountPath: /app/apps/api/data
          securityContext:
            allowPrivilegeEscalation: false
        - name: web
          image: ghcr.io/slashnephy/headlessx-web:2.0.4@sha256:ae2c295fef73c24f65a613a33a0e4ab72f2b427f70ce0337149f9d389301736f
          ports:
            - name: web
              containerPort: 3000
          env:
            - name: NEXT_PUBLIC_API_URL
              value: http://localhost:8000
            - name: NODE_ENV
              value: production
            - name: WEB_PORT
              value: "3000"
            - name: TZ
              value: Asia/Tokyo
          envFrom:
            - secretRef:
                name: headlessx-secret
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: models
          hostPath:
            path: /mnt/local/headlessx/models
            type: DirectoryOrCreate
        - name: data
          hostPath:
            path: /mnt/local/headlessx/data
            type: DirectoryOrCreate
      imagePullSecrets:
        - name: ghcr-io-credentials
      restartPolicy: Always
