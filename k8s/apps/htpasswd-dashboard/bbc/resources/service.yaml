apiVersion: v1
kind: Service
metadata:
  name: service

spec:
  selector:
    app: default
  type: NodePort
  ports:
    - name: "3000"
      port: 3000
      targetPort: 3000
      nodePort: 33000
      protocol: TCP
