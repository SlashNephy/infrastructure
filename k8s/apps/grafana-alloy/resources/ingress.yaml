apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: alloy

spec:
  routes:
    - kind: Rule
      match: Host(`grafana-alloy.starry.blue`)
      services:
        - name: alloy
          port: http-metrics
