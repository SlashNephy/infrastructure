apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: route

spec:
  routes:
    - kind: Rule
      match: Host(`sso.starry.blue`)
      services:
        - name: authentik-server
          port: 80
