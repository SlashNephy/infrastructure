apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: route

spec:
  routes:
    - kind: Rule
      match: Host(`muni-api.starry.blue`)
      services:
        - name: app
          port: 7090
