apiVersion: v1
kind: Service
metadata:
  name: app

spec:
  selector:
    app: default
  type: ClusterIP
  ports:
    - name: "40772"
      port: 40772
      targetPort: 40772
      protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  name: bbc
  annotations:
    tailscale.com/tailnet-fqdn: mahiron-bbc.spotted-halfmoon.ts.net

spec:
  type: ExternalName
  externalName: placeholder # generated

# 共有デバイスは OAuth 資格情報からアクセスできない
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: pink
#   annotations:
#     tailscale.com/tailnet-fqdn: pink.tail3fe75.ts.net

# spec:
#   type: ExternalName
#   externalName: placeholder # generated
