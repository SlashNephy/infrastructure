apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argo-cd

spec:
  routes:
    - kind: Rule
      match: Host(`argocd.starry.blue`)
      services:
        - name: argo-cd-argocd-server
          port: http
