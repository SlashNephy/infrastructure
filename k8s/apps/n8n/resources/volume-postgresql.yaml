apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: n8n-postgresql-data
spec:
  selector:
    matchLabels:
      name: n8n-postgresql-data
  storageClassName: local
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: n8n-postgresql-data
  labels:
    name: n8n-postgresql-data

spec:
  storageClassName: local
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 4Gi
  local:
    path: /mnt/local/n8n/postgresql
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - lily
  persistentVolumeReclaimPolicy: Retain
