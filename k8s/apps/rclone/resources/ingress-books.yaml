apiVersion: v1
kind: Service
metadata:
  name: books

spec:
  selector:
    app: books
  ports:
    - port: 5572
      targetPort: 5572

---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: books

spec:
  routes:
    - kind: Rule
      match: Host(`rclone.starry.blue`) && PathPrefix(`/books`)
      priority: 10
      services:
        - name: books
          port: 5572
      middlewares:
        - name: forward-auth-authentik
          namespace: authentik
        - name: rclone-authorization-header
