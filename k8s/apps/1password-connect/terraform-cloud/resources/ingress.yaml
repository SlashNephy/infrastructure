apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: route

spec:
  routes:
    - kind: Rule
      match: Host(`op-tfc.starry.blue`)
      services:
        - name: connect-api
          port: 8080
