apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: traefik

resources:
  - ./resources/helm
  - ./resources/namespace.yaml

configMapGenerator:
  - name: traefik-env
    literals:
      - TZ=Asia/Tokyo

generatorOptions:
  disableNameSuffixHash: false
