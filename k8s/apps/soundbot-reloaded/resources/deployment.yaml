apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment

spec:
  replicas: 1
  selector:
    matchLabels:
      app: default
  template:
    metadata:
      labels:
        app: default
    spec:
      containers:
        - name: soundbot-reloaded
          image: ghcr.io/slashnephy/soundbot-reloaded:master@sha256:44de1a3156ca80d1e189738a1dc936f98ecc00f4757d8ba37e8993596f5ceebe
          env:
            - name: DISCORD_TOKEN
              valueFrom:
                secretKeyRef:
                  name: soundbot-reloaded-secret
                  key: discord-bot-token
            - name: INITIAL_VOLUME
              value: "0.05"
          volumeMounts:
            - name: sounds
              mountPath: /app/sounds
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: sounds
          hostPath:
            path: /opt/k8s-volumes/soundbot/sounds
      restartPolicy: Always
