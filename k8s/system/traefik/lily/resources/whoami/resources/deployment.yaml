apiVersion: apps/v1
kind: Deployment
metadata:
  name: whoami

spec:
  replicas: 1
  selector:
    matchLabels:
      app: whoami
  template:
    metadata:
      labels:
        app: whoami
    spec:
      containers:
        - name: app
          image: ghcr.io/traefik/whoami:v1.11.0
          ports:
            - containerPort: 80
          securityContext:
            allowPrivilegeEscalation: false
