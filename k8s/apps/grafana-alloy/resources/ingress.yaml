apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: route

spec:
  routes:
    - kind: Rule
      match: Host(`grafana-alloy.starry.blue`)
      services:
        - name: http-metrics
          port: 12345
