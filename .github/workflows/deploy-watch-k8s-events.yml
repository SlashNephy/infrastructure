name: Deploy watch-k8s-events

on:
  push:
    paths:
      - cmd/watch-k8s-events/**
  workflow_dispatch:

permissions: {}

jobs:
  build:
    uses: SlashNephy/actions/.github/workflows/build-docker-image.yml@520bcf4b958627d8dbcd5207721f68324d208152 # v0.0.3
    permissions:
      contents: read
      packages: write
      actions: read
      security-events: write
    with:
      context: cmd/watch-k8s-events
      image-name: ghcr.io/slashnephy/watch-k8s-events
      image-platforms: linux/amd64
      dispatch-update-image-digest: true
    secrets:
      dispatch-github-token: ${{ secrets.BOT_GITHUB_TOKEN }}
