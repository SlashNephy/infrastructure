apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: route

spec:
  routes:
    - kind: Rule
      match: Host(`traefik.starry.blue`)
      services:
        - name: api@internal
          kind: TraefikService
