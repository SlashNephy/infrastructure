apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment

spec:
  replicas: 1
  selector:
    matchLabels:
      app: default
  template:
    metadata:
      labels:
        app: default
    spec:
      containers:
        - name: app
          image: ghcr.io/slashnephy/glance:main@sha256:be57e0d0f7e8c67af089f88d8af88e74488515879203234ac7e11de465c3b04d
          env:
            - name: TZ
              value: Asia/Tokyo
          envFrom:
            - secretRef:
                name: app-secret
          ports:
            - name: http
              containerPort: 8080
          volumeMounts:
            - name: config
              mountPath: /app/config
              readOnly: true
            - name: assets
              mountPath: /app/assets
              readOnly: true
      volumes:
        - name: config
          hostPath:
            path: /mnt/local/glance/config
            type: Directory
        - name: assets
          hostPath:
            path: /mnt/local/glance/assets
            type: Directory
      restartPolicy: Always
