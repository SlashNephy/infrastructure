apiVersion: batch/v1
kind: CronJob
metadata:
  name: cronjob

spec:
  schedule: "10 * * * *"
  jobTemplate:
    spec:
      backoffLimit: 0
      template:
        metadata:
          annotations:
            fluentbit.io/exclude: "true"
        spec:
          containers:
            - name: app
              image: ghcr.io/slashnephy/annict2anilist:master@sha256:a7ae43e2b59361a759c9fa83d535c3ced02d1dfb7f2e6803ac95bb235197af30
              env:
                - name: TOKEN_DIRECTORY
                  value: /app/token
                - name: LOG_LEVEL
                  value: DEBUG
              envFrom:
                - secretRef:
                    name: app-secret
              volumeMounts:
                - name: app-volume
                  mountPath: /app/token
              securityContext:
                allowPrivilegeEscalation: false
          volumes:
            - name: app-volume
              hostPath:
                path: /opt/k8s-volumes/annict2anilist-token
                type: DirectoryOrCreate
          restartPolicy: Never
  concurrencyPolicy: Forbid
