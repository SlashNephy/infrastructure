apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment

spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: default
  template:
    metadata:
      annotations:
        fluentbit.io/exclude: "true"
      labels:
        app: default
    spec:
      containers:
        - name: app
          image: jupyter/datascience-notebook:latest@sha256:466381d60e7e7e5130b791ccb34fb83f590d688847b534dad0a4cca843a0c04e
          workingDir: /home/jovyan/work
          command:
            - start-notebook.sh
          env:
            - name: DOCKER_STACKS_JUPYTER_CMD
              value: lab
            - name: GRANT_SUDO
              value: "true"
          volumeMounts:
            - name: work
              mountPath: /home/jovyan/work
            - name: config
              mountPath: /home/jovyan/.jupyter/jupyter_notebook_config.py
              subPath: jupyter_notebook_config.py
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: work
          hostPath:
            path: /opt/k8s-volumes/jupyter/work
            type: DirectoryOrCreate
        - name: config
          configMap:
            name: app-config
      restartPolicy: Always
      imagePullSecrets:
        - name: docker-hub-credentials
