apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment

spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: default
  template:
    metadata:
      annotations:
        fluentbit.io/exclude: "true"
      labels:
        app: default
    spec:
      containers:
        - name: app
          image: ghcr.io/slashnephy/releasechime:master@sha256:4fac673655edea17981b02bfe77de1722e895dfc84c547d8fd5c0f1a8cd37b18
          env:
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: releasechime-token
                  key: github-token
            - name: INTERVAL_SEC
              value: "600"
            - name: LOG_LEVEL
              value: TRACE
          envFrom:
            - secretRef:
                name: releasechime-secret
            - configMapRef:
                name: releasechime-config
            - configMapRef:
                name: releasechime-repositories-config
          volumeMounts:
            - name: cache
              mountPath: /app/data
          resources:
            limits:
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: cache
          hostPath:
            path: /opt/k8s-volumes/releasechime/cache
            type: DirectoryOrCreate
      restartPolicy: Always
