apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: route

spec:
  routes:
    - kind: Rule
      match: Host(`open-webui.starry.blue`)
      services:
        - name: open-webui
          port: http
